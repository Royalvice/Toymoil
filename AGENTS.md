# Toymoil 项目合同

Toymoil 是一款 cute-tech 2.5D 动作游戏。模板化和工具化只服务于游戏本身，
目的是让后续角色、敌人、武器、房间和 VFX 能由 Agent-first 工作流稳定扩展，
不是把 Toymoil 做成模板产品。

## main 分支事实

- main 是永久的产品基线，只包含文档、Skills、许可证和空目录骨架。
- main 不包含 .agent-os、project.godot、代码、场景、资产、测试、工具或运行证据。
- main 上不能宣称项目已运行、已实现或已通过视觉验收。
- 实现工作必须在开发分支进行；若该分支存在 .agent-os，按其索引恢复当前状态。

## 已确认的产品边界

- 世界是真实 3D 空间，X/Z 为地面、Y 为高度，比例保持 1:1:1。
- 玩家、敌人、武器、弹体和主要 VFX 使用 2D Sprite 表现。
- Sprite 在正式视角下不能暴露纸片侧面或产生异常形变。
- 地图允许组合 GridMap、Mesh、Decal 和 Sprite，并支持真实地形高度。
- 持久世界、相机、灯光和常驻角色应在 Godot 编辑器中可见、可选、可调整。
- 相机投影、角度、跟随方式、内部画面尺寸和像素密度尚未锁定。
- 战斗控制方式尚未锁定；不要预设移动与瞄准必须独立。

## 工程原则

- 游戏优先；先用可操作原型验证体验，再抽象可复用框架。
- 规则、视觉和内容数据保持职责清晰，但不提前发明复杂平台层。
- 模块通信优先直接调用、明确引用和局部 Signal；只有出现真实多方广播需求后，
  才设计事件系统。
- 运行时可以生成波次敌人、弹体、掉落和短生命周期 VFX，但对应模板必须能在
  编辑器中单独打开和预览。
- 禁止从空主场景在运行时构造整个可见世界。
- 不保留隐藏 fallback、第二套正式路线或与当前事实冲突的兼容层。

## 工作纪律

1. 先读本文件，再按 README 的顺序阅读 docs。
2. 进入开发分支后，如存在 .agent-os，继续读 project-index 和活动事项。
3. 保留用户和其他任务的无关改动，删除前解析精确目标。
4. 设计、编辑器可见、机器检查、运行验证和人工 Play Review 必须分开记录。
5. 没有证据时只能写 planned、in progress、unverified 或 blocked。
6. 产品决策变化时同步永久文档；实现状态只写入开发分支的 .agent-os。
7. 代码注释、日志和诊断文本使用英文；产品文档可以使用中文。

## Godot 证据

开发分支出现 Godot 项目后，本地启动、场景运行、调试输出和运行时检查使用
Godot MCP。MCP 不可用时如实标记 blocked；CI 可以使用固定版本 Godot headless。

## 许可

代码采用 GPL-3.0。美术优先使用 CC0-1.0 或公有领域内容；所有第三方与生成内容
都必须遵守 docs/08_ASSET_POLICY.md。
